# ─────────────────────────────────────────────────────────────────────────────
# AI Day Trading System — Environment Variables Template
#
# No API keys required! This system uses Yahoo Finance (free, no sign-up)
# and a built-in paper trading simulator.
#
# 1. Copy this file:  cp .env.template .env
# 2. Adjust any values below (or leave defaults as-is and just run it).
# ─────────────────────────────────────────────────────────────────────────────

# ── Paper Trading Simulator ───────────────────────────────────────────────────
# Virtual starting balance in USD
STARTING_BALANCE=10000.0

# ── Symbols to Scan ──────────────────────────────────────────────────────────
# Comma-separated list of US equity tickers (Yahoo Finance symbols)
SYMBOLS=AAPL,MSFT,TSLA,NVDA,SPY,QQQ,AMZN,GOOGL

# ── Scan Interval ────────────────────────────────────────────────────────────
# How often (seconds) to run a full scan cycle (300 = every 5 minutes)
SCAN_INTERVAL_SECONDS=300

# ── Risk Management ───────────────────────────────────────────────────────────
# Maximum account equity risked per trade (0.01 = 1%)
ACCOUNT_RISK_PER_TRADE=0.01

# Maximum total loss for the day before trading stops (0.03 = 3%)
MAX_DAILY_LOSS_PCT=0.03

# Maximum number of simultaneously open positions
MAX_OPEN_TRADES=3

# Minimum acceptable risk-reward ratio (e.g. 1.5 means 1.5:1)
MIN_RISK_REWARD=1.5

# ── Technical Analysis ────────────────────────────────────────────────────────
# Bars on each side required to confirm a swing high/low
SWING_LOOKBACK=5

# Total 5-minute bars to fetch per symbol per scan
LOOKBACK_BARS=100

# How close price must pull back to the trendline to count as a retest (0.3%)
RETRACEMENT_TOLERANCE=0.003
